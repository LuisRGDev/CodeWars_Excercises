function sortItOut(array){
//Write your own code!
    const input = array.split(' ').map(Number);
    const redondeados = input.map(numero => Math.floor(numero))
    let output = "";

    if(input.map % 2 !==0){
        
    }
}